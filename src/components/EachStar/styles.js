import { StyleSheet } from 'react-native';

export const FunctionStyles = (big) => StyleSheet.create({
    star: {
        width: grande ? 36 : 12,
        height: grande ? 36 : 12,
        marginRight: 2,
    }
});